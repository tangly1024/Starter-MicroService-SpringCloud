spring:
  application:
    name: biz-demo2
  cloud:
    config:
      profile: default
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka
server:
  port: 8081