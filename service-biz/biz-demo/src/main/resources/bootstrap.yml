spring:
  application:
    name: biz-demo
  cloud:
    config:
      profile: default
      discovery:
        enabled: true
        service-id: config-server
    bus:
      trace:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka
server:
  port: 8081
