spring:
  application:
    name: zipkin-server
  cloud:
    config:
      profile: default
      discovery:
        enabled: true
        service-id: config-server

server:
  port: 1003
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka
