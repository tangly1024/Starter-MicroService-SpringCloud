myconfig:
  tangly: default-value-bus-default

zuul:
  host:
    connect-timeout-millis: 10000
    socket-timeout-millis: 60000
  routes:
    # 测试外部链接
    external-baidu:
      path: /baidu/**
      url: https://www.baidu.com/
    external-tencent:
      path: /qq/**
      url: https://www.qq.com/
